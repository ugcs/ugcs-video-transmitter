<Window x:Class="VideoTransmitter.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VideoTransmitter.Views"
        xmlns:helpers="clr-namespace:VideoTransmitter.Helpers"
        xmlns:ffme="clr-namespace:Unosquare.FFME;assembly=ffme.win"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"          
	    xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro.Platform"  
        mc:Ignorable="d"
        Title="MainView" Height="720" Width="1280">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <cal:ActionMessage MethodName="ViewLoaded">
            </cal:ActionMessage>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Window.Resources>
        <Style x:Key="HiddenHeader" TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Visibility" Value="Collapsed" />
        </Style>
        <ControlTemplate x:Key="EmptyListBoxTemplate">
            <TextBlock Text="No Vehicles found" Foreground="White" FontSize="20" />
        </ControlTemplate>
        <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20 0 5 0">
                <Grid>
                    <ComboBox                               
                          SelectedItem="{Binding SelectedVideoSource}"
                          Width="200" 
                          Height="30" 
                          x:Name="SelectedVideoSourceElement"
                          DisplayMemberPath="Name" 
                          SelectedValuePath="Name"                       
                          SelectedValue="{Binding Path=Name}"
                          ItemsSource="{Binding Path=VideoSourcesList, Mode=OneWay}" >
                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Height" Value="30" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                            </Style>
                        </ComboBox.ItemContainerStyle>
                    </ComboBox>
                    <TextBlock
                        Margin="5 0 0 0"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        Visibility="{Binding SelectedItem, ElementName=SelectedVideoSourceElement, Converter={StaticResource NullToVisibilityConverter}}"
                        IsHitTestVisible="False"
                        Text="Select video source...  " />
                </Grid>
            </StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20 0 5 0">
                <Grid>
                    <ComboBox               
                              SelectedItem="{Binding SelectedVehicle}"
                              x:Name="SelectedVehicleElement"
                              Width="200" 
                              Height="30" 
                              DisplayMemberPath="Name" 
                              SelectedValuePath="VehicleId"             
                              SelectedValue="{Binding Path=VehicleId}"
                              ItemsSource="{Binding Path=VehicleList}" >
                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Height" Value="30" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                            </Style>
                        </ComboBox.ItemContainerStyle>
                    </ComboBox>
                    <TextBlock
                            Margin="5 0 0 0"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            Visibility="{Binding SelectedItem, ElementName=SelectedVehicleElement, Converter={StaticResource NullToVisibilityConverter}}"
                            IsHitTestVisible="False"
                            Text="Select vehicle...  " />
                </Grid>
            </StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20 0 5 0">
                <Button Content="Start streaming >" Width="150" Height="30">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <cal:ActionMessage MethodName="StartStreaming">
                            </cal:ActionMessage>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20 0 5 0">
                <Button Content="Settings" Width="150" Height="30">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <cal:ActionMessage MethodName="SettingsWindows">
                            </cal:ActionMessage>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
            </StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="UCS:" Margin="20 0 5 0" />
                <TextBlock Text="{Binding UcsConnection}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20 0 10 0">
                <TextBlock Text="VideoServer:" Margin="0 0 5 0" />
                <TextBlock Text="{Binding VideoServerConnection}" />
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <ffme:MediaElement x:Name="Media" LoadedBehavior="Play" UnloadedBehavior="Manual" MediaOpening="Media_MediaOpening"/>
            <TextBlock Text="{Binding VideoMessage}" VerticalAlignment="Center" x:Name="LoadingVideo" HorizontalAlignment="Center" Visibility="{Binding VideoMessageVisibility}" />
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Top" Margin="0 20 50 0">
                <TextBlock Text="{Binding VideoStreamStatus}" Background="White" Foreground="Black" FontSize="20" VerticalAlignment="Center" x:Name="VideoStatus" HorizontalAlignment="Right" Visibility="Visible" Margin="0 0 10 0" />
                <TextBlock Text="{Binding TelemetryStreamStatus}" Background="White" Foreground="Black" FontSize="20" VerticalAlignment="Center" x:Name="TelemetryStatus" HorizontalAlignment="Right" Visibility="Visible" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
